# Local config & build artifacts
.claude/
dist/
node_modules/

# Sorties de tests (Playwright / Lighthouse CI)
test-results/
playwright-report/
.lighthouseci/

# Original design bundle — kept locally for reference, not versioned
design_extracted/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Documents internes / livrables clients (gardés en local uniquement)
# remise-agence/ = dossier de clôture (montants, comptes, QR, PDF) — NE JAMAIS
# publier dans le dépôt public.
remise-agence/
briefing-*.md
contrat-*.md
budget-*.md
guide-*.md
multilingue-*.md
# Audits & plan : contiennent le détail de la faille de sécurité —
# NE JAMAIS publier tant que le correctif n'est pas appliqué côté ACT.
AUDIT-*.md
PLAN-ACTION.md
ETAT-PROJET-ACT.md
informations-act-en-attente.md
informations-act-en-attente.pdf
email-*.md
strategie_*.md
.docx_inspect/
*.docx
*.xlsx

# Bundle handoff design (référence locale)
*handoff.zip
"Tableau de bord admin ACT-handoff.zip"

# Photos brutes extraites du book ACT — à trier/renommer/optimiser avant
# intégration dans /images_du_senegal/. Conservées en local uniquement.
# (Pattern ancré à la racine pour ne pas exclure images_du_senegal/photos_act_book/
# qui contient les versions optimisées et catégorisées, à versionner.)
/photos_act_book/

# Seed Supabase généré (régénérable via les scripts export_*.py)
supabase/seed/seed_content.sql
supabase/seed/seed_content_full.sql

# Config Supabase locale (URLs staging, service_role key, jamais côté client)
supabase/.env
supabase/config.local.js
